# Final cleanup in the target: remove the live-session /etc/default/distro,
# which has no meaning on the installed system. Runs inside the target
# (dontChroot: false).
---
dontChroot: false
timeout: 10

script:
    - "rm -f /etc/default/distro"

i18n:
    name: "Remove live-session defaults"
